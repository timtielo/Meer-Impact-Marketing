---
import DesignLayout from '../../layouts/DesignLayout.astro';

const dirs = [
  {
    id: 1,
    name: 'Ad Manager',
    blurb: 'De hero is een live Meta Ads-dashboard. Performance-look, dashboard-esthetiek, cijfers voorop. Vertrouwen via data.',
    palette: ['#f2600c', '#0b1f3a', '#2563eb', '#eef1f6'],
    bg: 'linear-gradient(135deg,#eef1f6,#dbe5f5)',
  },
  {
    id: 2,
    name: 'Voor / Na',
    blurb: 'Harde VOOR/NA-split: grijs & vlak versus kleurrijk & stijgend. De before/after-transformatie als hele concept.',
    palette: ['#ea580c', '#12224e', '#e5e7eb', '#0f172a'],
    bg: 'linear-gradient(90deg,#e5e7eb 0 50%, #ea580c 50% 100%)',
  },
  {
    id: 3,
    name: 'Kinetic',
    blurb: 'Oversized bewegende typografie, warm-donker met hete oranje + marquee. Het meest gedurfd en onderscheidend.',
    palette: ['#ff5a1f', '#14100e', '#f7f2ea', '#1b1613'],
    bg: 'linear-gradient(135deg,#14100e,#2a2019)',
    dark: true,
  },
  {
    id: 4,
    name: 'Coach / Direct',
    blurb: 'Color-block editorial met een confronterende coach-vraag. No-nonsense, persoonlijk, karaktervolle display.',
    palette: ['#ea580c', '#101a3a', '#fbfaf7', '#12131a'],
    bg: 'linear-gradient(135deg,#ea580c,#101a3a)',
    dark: true,
  },
];
---
<DesignLayout title="Designrichtingen — Meer Impact Marketing" active={0}>
  <main class="hub">
    <header class="hub-head">
      <span class="kicker">Meer Impact Marketing · rebuild · ronde 2</span>
      <h1>Vier gedurfde richtingen</h1>
      <p>
        Meer lef dan de eerste ronde: elke richting neemt één echt risico en heeft een eigen "signature",
        gegrond in de wereld van Meta-ads en de fitnessbranche — geen brave oppoets-variant. Oranje + navy
        blijven het anker. Zelfde content per richting, zodat je puur de art-direction vergelijkt.
      </p>
    </header>

    <div class="grid">
      {dirs.map((d) => (
        <a href={`/ontwerp/richting-${d.id}`} class:list={['card', { dark: d.dark }]} style={`--card-bg:${d.bg}`}>
          <div class="card-preview">
            <span class="card-num">{d.id}</span>
            <div class="dots">
              {d.palette.map((c) => <span class="dot" style={`background:${c}`}></span>)}
            </div>
          </div>
          <div class="card-body">
            <h2>{d.name}</h2>
            <p>{d.blurb}</p>
            <span class="go">Bekijk richting {d.id} →</span>
          </div>
        </a>
      ))}
    </div>

    <footer class="hub-foot">
      <p>Zeg welke het beste voelt (of een mix / specifieke tweaks). Daarna bouw ik die richting uit tot het echte designsysteem en migreer ik alle pagina's — inclusief Meekijksessie-dienst, kennisbank en zoekfunctie.</p>
    </footer>
  </main>

  <style>
    .hub { max-width: 1120px; margin: 0 auto; padding: 3.5rem 1.5rem 5rem; font-family: ui-sans-serif, system-ui, -apple-system, 'Segoe UI', sans-serif; color: #0f172a; }
    .hub-head { max-width: 760px; margin-bottom: 3rem; }
    .kicker { font-size: 0.8rem; font-weight: 700; text-transform: uppercase; letter-spacing: 0.14em; color: #ea580c; }
    .hub-head h1 { font-size: clamp(2.4rem, 5vw, 3.4rem); font-weight: 900; letter-spacing: -0.03em; margin: 0.5rem 0 1rem; line-height: 1.05; }
    .hub-head p { color: #475569; font-size: 1.1rem; line-height: 1.6; }
    .grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 1.5rem; }
    @media (max-width: 760px) { .grid { grid-template-columns: 1fr; } }
    .card { display: flex; flex-direction: column; text-decoration: none; color: inherit; border: 1px solid #e2e8f0; border-radius: 18px; overflow: hidden; background: #fff; transition: transform 0.2s ease, box-shadow 0.2s ease, border-color 0.2s ease; }
    .card:hover { transform: translateY(-5px); box-shadow: 0 24px 48px -24px rgba(15, 23, 42, 0.35); border-color: #cbd5e1; }
    .card-preview { position: relative; height: 150px; background: var(--card-bg); display: flex; align-items: flex-end; justify-content: space-between; padding: 1.25rem; }
    .card-num { font-size: 3.5rem; font-weight: 900; letter-spacing: -0.05em; color: rgba(15, 23, 42, 0.18); line-height: 1; }
    .card.dark .card-num { color: rgba(255, 255, 255, 0.3); }
    .dots { display: flex; gap: 0.4rem; }
    .dots .dot { width: 22px; height: 22px; border-radius: 50%; border: 2px solid rgba(255, 255, 255, 0.6); }
    .card-body { padding: 1.5rem; }
    .card-body h2 { font-size: 1.5rem; font-weight: 800; letter-spacing: -0.02em; margin: 0 0 0.5rem; }
    .card-body p { color: #475569; font-size: 0.98rem; line-height: 1.55; margin: 0 0 1rem; }
    .go { font-weight: 700; color: #ea580c; }
    .hub-foot { margin-top: 3rem; padding: 1.5rem; background: #f8fafc; border-radius: 14px; border: 1px solid #e2e8f0; }
    .hub-foot p { margin: 0; color: #475569; }
  </style>
</DesignLayout>
